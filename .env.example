# Public app config (safe to expose to the browser).
NEXT_PUBLIC_APP_NAME=Mood Deck
NEXT_PUBLIC_APP_ENV=development

# Future server-side secrets (do NOT add until the feature lands).
# Keep these OUT of NEXT_PUBLIC_* so they never reach the client bundle.
# DATABASE_URL=
# STRIPE_SECRET_KEY=
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
